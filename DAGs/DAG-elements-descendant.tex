\documentclass[fleqn,reqno,10pt]{article}

\usepackage[]{mypackages}

\usepackage{mycommands}

\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{2pt}

\usetikzlibrary{calc}

\usepackage{pgfplots}

\begin{document}

\begin{tikzpicture}[
  node distance = 1.25cm,
  double distance = 2pt,
  minimum size=0.75cm,
  circle,
  thick]

  % NODES

  % anchor
  \node[] (anchor) {};

  % node X
  \node[draw=black, left of = anchor] (X) {$X$};

  % node Z
  \node[draw=black, right of = X] (Z) {$Z$};

  % node Y
  \node[draw=black, right of = anchor] (Y) {$Y$};

  % node A
  \node[draw=black, below of = Z] (A) {$A$};

  % ARROWS

  \draw[->] (X)--(Z);

  \draw[->] (Z)--(Y);

  \draw[->] (Z)--(A);

\end{tikzpicture}

\end{document}
