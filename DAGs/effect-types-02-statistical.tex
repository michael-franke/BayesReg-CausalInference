\documentclass[fleqn,reqno,10pt]{article}

\usepackage[]{mypackages}
\usepackage{mycommands}
\usepackage{CSPstyles}

\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{2pt}

\usetikzlibrary{calc}

\usepackage{pgfplots}

\begin{document}

% statistical effect: all ways in which knowing X may affect our predictions about Y
% purely correlational: insensitive to arrow direction

\begin{tikzpicture}[
  node distance = 1.25cm,
  double distance = 2pt,
  minimum size=0.75cm,
  circle,
  thick]

  % NODES

  % anchor
  \node[] (anchor) {};

  % node X
  \node[draw=black, left of = anchor] (X) {$X$};

  % node Y
  \node[draw=black, right of = anchor] (Y) {$Y$};

  % node Z1
  \node[draw=black, above of = anchor] (Z1) {$Z_{1}$};

  % node Z2
  \node[draw=black, below of = anchor] (Z2) {$Z_{2}$};

  % ARROWS causal

  \draw[->, color = independence-light-3] (X)--(Y);
  \draw[->, color = independence-light-3] (Z1)--(X);
  \draw[->, color = independence-light-3] (Z1)--(Y);
  \draw[->, color = independence-light-3] (X)--(Z2);
  \draw[->, color = independence-light-3] (Z2)--(Y);

  % ARROWS information flow

  \draw [->, ultra thick, color = CSP-emphasis ] (X) -- (Z1);
  \draw [->, ultra thick, color = CSP-emphasis ] (Z1) -- (Y);
  \draw [->, ultra thick, color = CSP-emphasis ] (X) -- (Y);
  \draw [->, ultra thick, color = CSP-emphasis ] (X) -- (Z2);
  \draw [->, ultra thick, color = CSP-emphasis ] (Z2) -- (Y);

  % \begin{scope}[transform canvas={yshift=.09cm, xshift=-.09cm}]
  %   \draw [->, color = CSP-emphasis ] (X) -- (Z1);
  % \end{scope}

  % \begin{scope}[transform canvas={yshift=.09cm, xshift=.09cm}]
  %   \draw [->, color = CSP-emphasis ] (Z1) -- (Y);
  % \end{scope}

  % \begin{scope}[transform canvas={yshift=.09cm}]
  %   \draw [->, color = CSP-emphasis ] (X) -- (Y);
  % \end{scope}

  % \begin{scope}[transform canvas={yshift=-.09cm, xshift=-.09cm}]
  %   \draw [->, color = CSP-emphasis ] (X) -- (Z2);
  % \end{scope}

  % \begin{scope}[transform canvas={yshift=-.09cm, xshift=.09cm}]
  %   \draw [->, color = CSP-emphasis ] (Z2) -- (Y);
  % \end{scope}


\end{tikzpicture}

\end{document}
